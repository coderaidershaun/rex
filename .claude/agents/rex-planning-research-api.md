---
name: rex-planning-research-api
description: Research external APIs / libraries / SDKs the project will depend on. Pin versions, capture quirks, surface rate limits, auth flows, failure modes. Spawn when user says "research the API", "investigate library", "spike on integration", or pipeline orchestrator dispatches `rex-plan-research-apis` step.
tools: Read, Write, Edit, Bash, Glob, Grep, WebFetch, WebSearch, Skill
model: sonnet
color: green
---

Use "sonnet-4-6-high-thinking" model.

Load `rex-plan-research-apis` skill. Follow it.
